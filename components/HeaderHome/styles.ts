import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      alignItems: 'center',
      backgroundColor:'#000',
      flexDirection:'row',
      paddingTop:15
    },
    textHeader:{
        color:'white',
        fontSize: 20,
        fontWeight:'700',
    }
  });

export default styles;